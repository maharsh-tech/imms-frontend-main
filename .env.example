# Backend API base URL (include /api/v1 prefix)
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Optional — show Continue with Google on login (requires backend GOOGLE_* env)
VITE_GOOGLE_AUTH=false

# Optional — dev role switcher on login page (local/staging only)
VITE_DEV_AUTH=false
